// Geo-lock temporarily disabled. Always returns true.
export const isUserWithinIntramuros = () => true;
/**
 * Check if a user's location is within the Intramuros boundaries.
 * TEMPORARILY returns true to deactivate geo-lock restrictions.
 * Revert this change when geo-lock needs to be re-enabled.
 */
